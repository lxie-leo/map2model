# 把这个文件复制到 map2model.exe 旁边、改名成 map2model.env 就能生效。
# 一行一个 KEY=VALUE,# 开头是注释。改完重启程序。

# Blender 装在非默认位置时(自动探测只认 PATH 和 C:\Program Files\Blender Foundation):
# M2M_BLENDER_PATH=D:\Blender\blender.exe

# 数据库、任务产物、日志默认在 %LOCALAPPDATA%\map2model,想挪个地方:
# M2M_DATA_DIR=D:\map2model-data

# 同时跑几个生成任务(默认 2):
# M2M_TASK_CONCURRENCY=2
